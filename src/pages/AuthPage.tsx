import LoginPage from '@/pages/LoginPage';

function AuthPage() {
    return (
        <section className="flex justify-center items-center h-[calc(100vh-5rem)]">
            <LoginPage />
        </section>
    );
}

export default AuthPage;
